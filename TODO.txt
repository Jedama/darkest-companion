=== LLM infra ===

Tiered models: route simple/mechanical LLM tasks (e.g. pose-tagging a finished
dialogue) to a cheaper/faster model instead of the estate's configured model.
No per-call model override exists yet — callEstateLLM/EstateLLMOptions only
take temperature/system/effort/label, model always comes from
estate.preferences?.llmModel.

Retry malformed structured LLM output instead of failing outright. E.g. if a
pose-tagging pass returns indexes that don't match the transcript, currently
there's nothing to retry with — every bad response is a hard failure (or,
for poses specifically, would just fall back to neutral). No retry mechanism
of any kind exists anywhere in the LLM layer yet.

=================================================

Make a way to add locations later. Butchers and nomad's shouldn't be here yet

Make the return event more likely to add traits or notes (Maybe get the traits) or instruct it to add any throughline from the expedition, if any. 

Way to update residences

Make the recruit less prone to +1's!!!!!

Fix the recruit housing. And not having a proper house must also be possible. Just roaming or hiding. (DONE?)
Make restorable buildings work, with a explanation at the top, and (Status: Currently ruined and non-functional) or (Status: Restored and operational)


Mention days being not linear and fill the gaps. I guess

Add Scarlett Academy to the locations??? 

Scale the number of planning meeting lines based on attendees?

Add "Ends every formal meeting with 'Furthermore, the Condemned Shack must be destroyed!'" to exterminator once the condemned shack is rebuilt.

When money is tracked, add to recruit if low? Or in general? 